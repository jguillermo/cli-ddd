import { INestApplication } from '@nestjs/common';
import { TestingE2eModule } from '../../testing-e2e-module';
import { <%= classRepository %> } from '../../../src/<%= aggregatePropertie %>/domain/<%= fileRepository %>';

export interface <%= testingInterface %> {
  app: INestApplication;
  <%= aggregatePropertieRepository %>: <%= classRepository %>;
}

export class <%= className %> extends TestingE2eModule {
  static async create(): Promise<CreateTestingInterface> {
    const module = new <%= className %>();
    await module.init();
    return {
      app: module.app,
      <%= aggregatePropertieRepository %>: module.moduleFixture.get<<%= classRepository %>>(<%= classRepository %>),
    };
  }
}
