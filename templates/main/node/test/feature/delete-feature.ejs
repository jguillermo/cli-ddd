Feature: Delete <%= aggregate.name.value %>
  In order to delete <%= aggregatePropertie %>
  As an user
  I need to be able to delete <%= aggregate.name.value %>

  Background:
  Given I have the following data on collection "companies"
  """
    [
      {
        "id": "daf673b7-b1ba-415e-ac5e-04848e5e2e5f",
        "name": "<%= aggregate.name.value %> A"
      }
    ]
    """


  Scenario: delete one <%= aggregatePropertie %>
    Given I make a request to graphql
    """
    mutation{
      <%= graphQlService %>(id:"daf673b7-b1ba-415e-ac5e-04848e5e2e5f"){
        status
      }
    }
    """
    And I validate the response is
    """
    {
      "data": {
        "<%= graphQlService %>": {
            "status": "ok"
         }
      }
    }
    """
    Then response should have a status 200


  Scenario: delete <%= aggregatePropertie %> not exist
    Given I make a request to graphql
    """
    mutation{
      <%= graphQlService %>(id:"daf673b7-b1ba-415e-ac5e-04848e5e2e6f"){
        status
      }
    }
    """
    And I validate the response is
    """
    {
      "data": {
        "<%= graphQlService %>": {
            "status": "ok"
         }
      }
    }
    """
    Then response should have a status 200
