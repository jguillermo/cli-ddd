import { EventBase } from 'base-ddd';

export class <%= className %> extends EventBase {
  constructor(<%= strVoUndescoreProperties %>) {
    super();
  }
  <%_ propertiesEvent.forEach(function(e){ _%>

  get <%= e.e.propertie.name.value %>(): <%= e.ePrimitive %> {
    return this._<%= e.e.propertie.name.value %>;
  }
  <%_ }); _%>

  eventName(): string {
    return '<%= eventType %>';
  }
}
