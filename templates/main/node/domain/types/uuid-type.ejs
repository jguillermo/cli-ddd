import { UUIDType } from 'base-ddd';

export class <%= className %> extends UUIDType {
  <%_ if (propertie.propertie.required.value) { _%>
  isValid(): boolean {
    if (this.isNull) {
      throw new Error('is required.');
    }
    return true;
  }
  <%_ } _%>
}
