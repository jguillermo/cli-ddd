import { StringType } from 'base-ddd';

export class <%= className %> extends StringType {
<%_ if (propertie.propertie.required.value) { _%>
  isValid(): boolean {
    if (this.isNull) {
      throw new Error('is required.');
    }
    if (this.isEmpty) {
      throw new Error('should not be empty.');
    }
    return true;
  }
<%_ } _%>
}

