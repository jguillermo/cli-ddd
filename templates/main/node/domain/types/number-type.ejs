import { NumberType } from 'base-ddd';

export class <%= className %> extends NumberType {
  <%_ if (propertie.propertie.required.value) { _%>
  isValid(): boolean {
    if (this.isNull) {
      throw new Error('is required.');
    }
    return true;
  }
  <%_ } _%>
}
